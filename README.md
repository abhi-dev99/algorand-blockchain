# Algorand Blockchain Info

This repository contains a simple Python script to fetch functionality from the Algorand blockchain.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the script:
   ```bash
   python algorand_info.py
   ```
